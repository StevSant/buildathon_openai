import { PresentationDeck } from "@/components";

export const metadata = {
  title: "Presentaciones | Pulso",
  description: "Una colección visual independiente inspirada en Pulso.",
};

export default function PresentacionesPage() {
  return <PresentationDeck />;
}
