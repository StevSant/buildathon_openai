/**
 * OpenAI's canonical REST base. Named constant (not an inline literal) and always
 * overridable via adapter config (OPENAI_BASE_URL).
 */
export const OPENAI_DEFAULT_BASE_URL = 'https://api.openai.com/v1';
